---
const { twColor = "text-gallery-400 dark:text-shark-300", classes } =
  Astro.props;
---

<h2
  class={["uppercase text-2xl md:text-3xl font-bold", twColor, classes].join(
    " "
  )}>
  <slot />
</h2>
